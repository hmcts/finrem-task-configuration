#!groovy

properties([
  pipelineTriggers([cron('H 8 * * 1-5')]),
])

@Library('Infrastructure')

def type = "java"
def product = "finrem"
def component = "task-configuration"

withPipeline(type, product, component) {
  afterAlways('smokeTest') {
    junit(testResults: '**/build/test-results/smokeTest/*.xml', allowEmptyResults: true)
  }
}
